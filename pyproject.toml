[project]
name = "llm-2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter",
    "minsearch",
    "openai",
    "python-dotenv",
    "requests",
    "sqlitesearch>=0.1.1",
]
